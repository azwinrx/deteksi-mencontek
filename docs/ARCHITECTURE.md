# 🏗️ SYSTEM ARCHITECTURE

## 📐 Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                       CYBER PROCTOR SYSTEM                       │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE                           │
├─────────────────────────────────────────────────────────────────┤
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │  OpenCV GUI  │  │ Dashboard UI │  │ Matplotlib   │         │
│  │  (Real-time) │  │ (Web Browser)│  │ (Graphs)     │         │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘         │
└─────────┼──────────────────┼──────────────────┼─────────────────┘
          │                  │                  │
┌─────────┼──────────────────┼──────────────────┼─────────────────┐
│         │    APPLICATION LAYER                │                 │
├─────────┼──────────────────┼──────────────────┼─────────────────┤
│         ▼                  ▼                  ▼                 │
│  ┌─────────────┐    ┌──────────────┐   ┌─────────────┐        │
│  │   main.py   │    │ dashboard.py │   │ visualize_  │        │
│  │             │    │              │   │ timeline.py │        │
│  │ • Detection │    │ • Flask API  │   │             │        │
│  │ • Tracking  │    │ • REST       │   │ • Plotting  │        │
│  │ • Alert     │    │ • WebSocket  │   │ • Analysis  │        │
│  │ • Screenshot│    │              │   │             │        │
│  └──────┬──────┘    └──────┬───────┘   └──────┬──────┘        │
└─────────┼──────────────────┼──────────────────┼─────────────────┘
          │                  │                  │
┌─────────┼──────────────────┼──────────────────┼─────────────────┐
│         │    PROCESSING LAYER              │                    │
├─────────┼──────────────────┼──────────────────┼─────────────────┤
│         ▼                  │                  │                 │
│  ┌─────────────────────────┼──────────────────┼──────┐         │
│  │  Computer Vision Engine │                  │      │         │
│  │  ┌────────────┐  ┌──────▼──────┐  ┌───────▼────┐ │         │
│  │  │ MediaPipe  │  │   OpenCV    │  │ Matplotlib │ │         │
│  │  │ Face Mesh  │  │  Face Rec   │  │   pyplot   │ │         │
│  │  └────────────┘  └─────────────┘  └────────────┘ │         │
│  └──────────────────────┬──────────────────────────────┘         │
└─────────────────────────┼──────────────────────────────────────┘
                          │
┌─────────────────────────┼──────────────────────────────────────┐
│                         ▼    DATA LAYER                         │
├─────────────────────────────────────────────────────────────────┤
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │    JSON      │  │  Screenshots │  │ Face Models  │         │
│  │   Storage    │  │   (Images)   │  │   (LBPH)     │         │
│  │              │  │              │  │              │         │
│  │ data/*.json  │  │ screenshots/ │  │ known_faces/ │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
└─────────────────────────────────────────────────────────────────┘
```

## 🔄 Data Flow

```
┌─────────────┐
│   Camera    │
│   Input     │
└──────┬──────┘
       │
       ▼
┌─────────────────────────┐
│  Frame Capture & Flip   │
└──────┬──────────────────┘
       │
       ▼
┌─────────────────────────┐
│  RGB Conversion         │
└──────┬──────────────────┘
       │
       ▼
┌─────────────────────────┐
│  MediaPipe Face Mesh    │
│  (468 landmarks)        │
└──────┬──────────────────┘
       │
       ├──────────────────────────┐
       │                          │
       ▼                          ▼
┌──────────────┐         ┌────────────────┐
│ 3D Pose      │         │ Face           │
│ Estimation   │         │ Recognition    │
│ (solvePnP)   │         │ (LBPH)         │
└──────┬───────┘         └────────┬───────┘
       │                          │
       ├──────────────────────────┤
       │                          │
       ▼                          ▼
┌─────────────────────────────────────┐
│  Cheat Detection Logic              │
│  • Left/Right turn > thresh_y       │
│  • Head down > thresh_x             │
│  • Identity verification            │
└──────┬──────────────────────────────┘
       │
       ├─── YES (Cheating) ───┐
       │                      │
       ▼                      ▼
┌──────────────┐      ┌────────────────┐
│ Counter++    │      │ Trigger Alert  │
└──────┬───────┘      └────────┬───────┘
       │                       │
       ▼                       ▼
┌──────────────┐      ┌────────────────┐
│ Screenshot   │      │ Sound Beep     │
└──────┬───────┘      └────────┬───────┘
       │                       │
       └───────────┬───────────┘
                   │
                   ▼
          ┌────────────────┐
          │ Log Timeline   │
          │ (JSON)         │
          └────────┬───────┘
                   │
       ┌───────────┼───────────┐
       │           │           │
       ▼           ▼           ▼
┌──────────┐ ┌─────────┐ ┌──────────┐
│Dashboard │ │Timeline │ │Evidence  │
│Update    │ │Graph    │ │Files     │
└──────────┘ └─────────┘ └──────────┘
```

## 🎯 Component Breakdown

### 1. Detection Module (`main.py`)

```
Input: Video frame
│
├─▶ Face Mesh Detection (MediaPipe)
│   └─▶ 468 facial landmarks
│
├─▶ Pose Estimation (OpenCV solvePnP)
│   └─▶ Head orientation angles (x, y, z)
│
├─▶ Face Recognition (LBPH)
│   └─▶ Identity verification
│
├─▶ Cheat Detection Logic
│   ├─▶ Compare angles vs thresholds
│   └─▶ Cooldown timer (2s)
│
└─▶ Output: Status, Alerts, Screenshots
```

### 2. Analytics Module (`visualize_timeline.py`)

```
Input: JSON session data
│
├─▶ Load latest/specific session
│
├─▶ Parse timeline data
│   ├─▶ Extract timestamps
│   ├─▶ Extract violation types
│   └─▶ Calculate distributions
│
├─▶ Generate Visualizations
│   ├─▶ Scatter plot (timeline)
│   ├─▶ Bar chart (distribution)
│   └─▶ Statistics summary
│
└─▶ Output: Interactive graphs + PNG
```

### 3. Dashboard Module (`dashboard.py`)

```
Input: HTTP requests
│
├─▶ Flask Routes
│   ├─▶ / (index page)
│   ├─▶ /api/sessions (all data)
│   ├─▶ /api/latest (current)
│   └─▶ /api/stats (aggregated)
│
├─▶ Data Processing
│   ├─▶ Read JSON files
│   ├─▶ Calculate statistics
│   └─▶ Format responses
│
├─▶ Template Rendering
│   └─▶ dashboard.html (responsive UI)
│
└─▶ Output: Web dashboard (http://localhost:5000)
```

### 4. Registration Module (`register_face.py`)

```
Input: User webcam + name
│
├─▶ Capture 30 photos
│   └─▶ From various angles
│
├─▶ Train LBPH Model
│   ├─▶ Extract features
│   └─▶ Build classifier
│
├─▶ Save Model
│   ├─▶ face_model.yml
│   └─▶ face_data.pkl (metadata)
│
└─▶ Output: Recognition system ready
```

## 🔐 Security Features

```
┌────────────────────────┐
│  Security Layers       │
├────────────────────────┤
│  1. Face Recognition   │
│     └─▶ Anti-joki      │
│                        │
│  2. Identity Verify    │
│     └─▶ Every 5s check│
│                        │
│  3. Screenshot Evidence│
│     └─▶ Timestamped    │
│                        │
│  4. Timeline Logging   │
│     └─▶ Immutable JSON │
└────────────────────────┘
```

## 📊 Monitoring Flow

```
Exam Session Running
        │
        ▼
   ┌────────┐
   │Detection│◀─────┐
   │ Active  │      │
   └────┬───┘      │
        │          │
        ▼          │
   Violation?      │
    │        │     │
   YES      NO     │
    │        │     │
    ▼        └─────┘
┌─────────┐
│ Alert + │
│Screenshot│
└────┬────┘
     │
     ▼
┌──────────┐
│Save Data │
└────┬─────┘
     │
     ▼
┌──────────────────┐
│Dashboard Update  │
│(Real-time)       │
└──────────────────┘
```

## 🚀 Deployment Architecture

```
┌─────────────────────────────────────┐
│         Local Machine               │
├─────────────────────────────────────┤
│                                     │
│  ┌──────────────┐  ┌─────────────┐ │
│  │  Detection   │  │  Dashboard  │ │
│  │  Process     │  │  Server     │ │
│  │  (OpenCV)    │  │  (Flask)    │ │
│  └──────┬───────┘  └──────┬──────┘ │
│         │                 │        │
│         ▼                 ▼        │
│  ┌─────────────────────────────┐  │
│  │     Shared Data Storage     │  │
│  │  • data/                    │  │
│  │  • screenshots/             │  │
│  │  • known_faces/             │  │
│  └─────────────────────────────┘  │
└─────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│      Browser Access                 │
│      http://localhost:5000          │
└─────────────────────────────────────┘
```

## 💾 Storage Schema

```
data/
└── session_YYYYMMDD_HHMMSS.json
    {
      "timestamp": "string",
      "durasi_total": float,
      "jumlah_mencontek": int,
      "user_name": "string",
      "timeline": [
        {
          "waktu": float,
          "jenis": "string",
          "timestamp": "ISO8601"
        }
      ]
    }

screenshots/
└── YYYYMMDD_HHMMSS_STATUS.jpg
    (BGR image, 640x480 or camera resolution)

known_faces/
├── face_model.yml (OpenCV LBPH model)
└── face_data.pkl
    {
      "name": "string",
      "total_images": int,
      "label": int
    }
```

---

**This architecture supports:**

- ✅ Real-time detection
- ✅ Multi-session tracking
- ✅ Evidence collection
- ✅ Identity verification
- ✅ Web-based monitoring
- ✅ Data analytics
- ✅ Scalable design
